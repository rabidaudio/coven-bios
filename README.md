Little bios for the members of The Coven, for putting on the wall (and possibly a future website).

# Make your own

1. Setup project `npm install`
2. Add your info to `members.yml`
3. `npm run build`
